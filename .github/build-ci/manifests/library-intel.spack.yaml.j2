spack:
  specs:
  - cable@git.{{ ref }} ~mpi library=access-esm1.6

  packages:
    intel-oneapi-compilers-classic:
      require:
      - '@2021.10.0'

    all:
      require:
      - '%access_intel'
      - target={{ target }}

  concretizer:
    unify: false
  view:false
