spack:
  specs:
  - cable@git.{{ ref }} ~mpi library=access3

  packages:
    intel-oneapi-compilers-classic:
      require:
      - '@{{ oneapi_compiler_classic_ver }}'

    all:
      require:
      - '%access_intel'
      - 'target={{ target }}'

  concretizer:
    unify: false
  view: false
